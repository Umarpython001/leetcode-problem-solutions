  # 2265. Count Nodes Equal to Average of Subtree

                                        ## Solution

                                        ```
                                                        if (not curr) or (curr in seen):
                continue
            
            pot = bfs(curr)
            if pot == curr.val:
                ret.append(pot)
            
            left = curr.left


            print(f"Currently on {curr.val}")
                print(f"{curr.val} satisfies")
            if left not in seen:
                que.append(left)

            if right not in seen:
                que.append(right)
            right = curr.right
                                        ```;


                    