  # 496. Next Greater Element I 
        
    ## Solution

    ```
                        ans.append(nums2[j_index])
                    break
                else:
                    j_index+=1
                    if j_index-1 == n2-1:
                        ans.append(-1)



        print(ans) 
                if nums2[j_index] > num:
            while j_index < n2: # We want to check all elements to the right of j_index

            element
            j_index+=1 # Increase j_index bcos we are going to start checking from the next
    ```;


                    